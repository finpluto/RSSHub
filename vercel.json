{
    "framework": null,
    "buildCommand": "tsc --project ./tsconfig.json && resolve-tspaths --project ./tsconfig.json",
    "outputDirectory": ".",
    "github": { "silent": true },
    "routes": [{ "src": "/.*", "dest": "/dist/api/vercel.js" }]
}
